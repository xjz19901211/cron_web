h1 Listing works

table
  thead
    tr
      th Name
      th Input args
      th Code lang
      th Code preview
      th
      th
      th
      th

  tbody
    - @works.each do |work|
      tr
        td
          strong = work.name
        td = work.input_args
        td = work.code_lang
        td
          pre = work.code[0..128]
        td = link_to 'Show', work
        td = link_to 'Edit', edit_work_path(work)
        td = link_to 'Schedule', new_work_schedule_path(work)
        td = link_to 'Destroy', work, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Work', new_work_path
